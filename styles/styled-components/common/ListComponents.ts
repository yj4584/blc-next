import styled from 'styled-components';

export const RowSection = styled.section`
	position: relative;
	width: 100%;
	margin-top: 10px;
	margin-bottom: 10px;
`;

export const TitleSection = styled.h4`
	font-size: 22px;
	font-weight: 700;
	margin: 0;
	margin-bottom: 30px;
`;

export const TitleButton = styled.a`
	position: absolute;
	right: 0;
	top: 0;
	font-size: 0.875rem;
	font-weight: 400;
	padding: 0.5rem 0.5rem;
	line-height: 1.5;
	border-radius: 0.2rem;
	background-color: #007bff; //#283381
	border-color: #0a58ca;
	color: #fff;
`;
